# RANSAC Demos

This folder contains two RANSAC demo variants:

- `modal_fit_sweep/`: modal fitting from sweep data.
- `homography_implement/`: MATLAB re-implementation of homography estimation with RANSAC.

Each variant owns its own `scripts/`, `functions/`, `data/`, and `figures/` folders.

