# 2026-W35 Short-Term Plan

## Scope

Phase A of `projects/learning_based_manipulation_project.md`: behavior cloning and multimodal action distributions. Expected effort 4–6 focused hours.

## Session 1 — BC objective and its optimum

Status: not started.

Goals:

- Derive that \(\mathcal L_{\mathrm{MSE}}=\mathbb E\|a-\pi(o)\|^2\) is minimized by \(\pi^\star(o)=\mathbb E[a\mid o]\).
- Show with a two-mode manipulation example why the conditional mean can be an invalid robot command.
- Write down the covariate-shift / compounding-error argument in my own words.

Update target: `topics/behavior_cloning.md`.

## Session 2 — Toy multimodal BC demo

Status: not started.

Goals:

- Build a 2-D dataset where the same observation admits \(a\approx[-1,1]^T\) or \(a\approx[1,1]^T\).
- Train a deterministic MLP under MSE and confirm it collapses toward \(a\approx[0,1]^T\).
- Plot dataset, prediction, and the invalid averaged action.

Update targets: `demo/behavior_cloning/scripts/`, `demo/behavior_cloning/figures/`.

## Session 3 — Probabilistic policies

Status: not started.

Goals:

- Gaussian policy \(\mathcal N(\mu_\theta(o),\Sigma_\theta(o))\) and its NLL objective.
- Separate epistemic uncertainty, stochasticity, and genuine expert multimodality.
- State why a single Gaussian is still insufficient — the motivation for diffusion.

## Exit criterion for the week

I can explain without notes why robot imitation learning should model a distribution over actions rather than one deterministic action, and mark milestone 1 of the project as complete.

## Plan policy

Short-term plan files live under `plan/<year>/short_term/` and use year plus ISO week number.
