# 2026-W19 Short-Term Plan

## Scope

Finish the MATLAB algorithm demos for:

- RANSAC modal-fit algorithm.
- L-BFGS optimization algorithm.

## RANSAC modal-fit demo

Status: planned, waiting for user-provided code.

Goals:

- Adapt the RANSAC workflow to a modal fitting problem.
- Define the modal model parameters and residual function.
- Use minimal random samples to estimate candidate modal parameters.
- Build a consensus set with residual threshold `tau`.
- Refit the best modal model using all inliers.
- Save plots and notes under `demo/ransac/`.

Expected update targets:

- `demo/ransac/scripts/run_ransac_demo.m`
- `demo/ransac/functions/`
- `topics/ransac.md`

## L-BFGS demo

Status: planned.

Goals:

- Keep the current Rosenbrock demo as the basic optimizer check.
- Add or adapt an L-BFGS example for the same fitting/optimization workflow if useful.
- Document the objective function, gradient, line search, and convergence behavior.
- Save plots and notes under `demo/l_bfgs/`.

Expected update targets:

- `demo/l_bfgs/scripts/run_l_bfgs_demo.m`
- `demo/l_bfgs/functions/`
- `topics/l_bfgs.md`

## Plan policy

This is a short-term weekly plan. Short-term plan files live under `plan/<year>/short_term/` and use year plus ISO week number as the filename, such as `2026-W19.md`.

